extends ../layout

block content
  h1 Login
  p(style="color:blue")= error
  form(action="/login",method="post")        
        .form-group
            label(for="email") Email
            input#email(type="text", class="form-control", name="email" value="shehbaztariq1988@gmail.com")
        .form-group
            label(for="password") Password
            input#price(type="password", class="form-control", name="password", value="qwerty")
        input(type="submit", class="btn btn-info" value="Login")
